function ModifyCube(cubeObj)
    -- 获取Unity的Vector3类型
    local Vector3 = CS.UnityEngine.Vector3
    -- 修改缩放
    cubeObj.transform.localScale = Vector3(1.75, 1.35, 2)
    print("Lua热更生效：Cube已缩小至50%")
end